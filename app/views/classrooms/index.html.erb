<h1>Lista de Matrículas</h1>
<% if (@students_count > 0 && @courses_count > 0)%>
	<%= link_to "Nova Matrícula", new_classroom_path, class: "btn btn-success" %>
	<%= render 'list_enrolment' %>
<% else %>
<div class="jumbotron">
	<h2>Você não precisa cadastrar pelo menos um estudante e um curso!</h2>
</div>
<% end %>