<%= simple_form_for @classroom do |f| %>
	<%= f.input :student_id, label: "Nome: ", :collection => Student.order(:name) %>
	<%= f.input :course_id, label: "Descrição: ", :collection => Course.order(:name) %>
	<%= f.submit "Salvar" %>
<% end %>